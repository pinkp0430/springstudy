SQL(ORACLE)

SELECT vet_id FROM vet_specialties WHERE specialty_id = 2; 
SELECT * FROM vets WHERE ID IN (2, 4, 6, 9);

SELECT * FROM vets WHERE ID IN (SELECT vet_id FROM vet_specialties WHERE specialty_id = 2);

서브쿼리 작성시
1. 연산자의 오른쪽에 위치
2. 괄호 처리
3. 다중행 서브쿼리 (서브쿼리의 결과가 여러줄인 경우)
IN			= 같은값
<			< ALL(2,3,4)  	: 2 보다 작은 : and 개념이다. 2,3,4 중 작은이니, 2보다 작은 		
>			> ALL(2,3,4)	: 4 보다 큰
ANY			< ANY (2,3,4)	: 4 보다 작은 : or. 2,3,4 중 보다 작은이니, 4보다 작은
ALL 			> ANY (2,3,4)	: 2 보다 큰

CREATE SEQUENCE VETS_SEQ START WITH 10;  /* 이미 들어가잇는 데이터 다음부터 카운트 */
시퀀스 테이블을 잘못 만들엇으면 DROP 시키고 다시 만들어야한다.