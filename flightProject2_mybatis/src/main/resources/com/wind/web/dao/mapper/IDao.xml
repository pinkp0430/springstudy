<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wind.web.dao.IDao">

<!-- airportTB -->
    <select id="airport_listDao" resultType="com.wind.web.dto.AirportDto">
		SELECT port_continent, port_country, port_name, port_code FROM airportTB  <!-- 공항 리스트를 가져와라  -->
	</select>  
	<insert id="inputAirportDao">
		insert into airportTB(port_continent, port_country, port_code, port_name) values (#{param1}, #{param2}, #{param3}, #{param4})
	</insert>
	<delete id="airport_deleteDao">
		delete from airportTB where port_code = #{param1}
	</delete>
<!-- ticketTB -->
	<select id="ticket_listDao" resultType="com.wind.web.dto.TicketDto">
		SELECT * FROM ticketTB  <!-- 공항 리스트를 가져와라  -->
	</select>  
	<insert id="inputTicketDao">
		insert into ticketTB(ticket_id, airline, flight_name, departure, arrive, dep_time, arri_time, seat_able, seat_price, dName, aName) 
		values (ticketTB_seq.nextval, #{param1}, #{param2}, #{param3}, #{param4}, #{param5}, #{param6}, #{param7}, #{param8}, #{param9}, #{param10})
	</insert>
	<delete id="ticket_deleteDao">
		delete from ticketTB where ticket_id = #{param1}
	</delete>
		<select id="getPort_nameDao" resultType="String">
		SELECT port_name FROM airportTB where port_code = #{param1} <!-- 공항 리스트를 가져와라  -->
	</select> 
	
</mapper>
